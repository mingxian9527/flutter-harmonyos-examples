name: image_widget_demo
description: Image Widget 演示项目 - 展示图片加载方式（网络、Asset、文件）

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 图片资源配置
  assets:
    - assets/images/
